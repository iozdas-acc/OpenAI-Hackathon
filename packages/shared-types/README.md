Shared types and contracts live here.

Review workspace payloads for the frontend and backend live in:

- `src/review-workspace.ts`
- `examples/review-workspace.example.json`

The backend is expected to provide:

- compressed review context
- evidence cards
- nearby mappings
- the active decision with proposed options and actions
- queue state
- outcome snapshots
- reviewer note placeholder or persisted draft
